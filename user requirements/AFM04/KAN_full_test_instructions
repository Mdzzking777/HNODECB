AFM04 KAN full test instructions

目的：

在 AFM04 中建立一个与主 `stage1pluslight` 解耦的 KAN 完整功能性测试环境，用来验证：

1. 在 `x1, x2, x3` 作为输入时，KAN 能否输出足够准确的 `Fts / F_contact`；
2. 在其他物理项均视为已知的前提下，将该输出代入已知 RHS 后，整段 rollout 是否能得到低 loss；
3. 在这种“模块化嵌入 HNODE”语境下，KAN 的一些设置细节（例如 `noise_scale`、adaptive grid）是否可行。

当前敲定的要求：

1. 整个测试只放在：

   `AFM04/KAN_full_test`

   内部单独分类实现，不污染 `AFM04/stage1pluslight` 现有功能。

2. KAN 使用本地 `pykan`。

3. KAN 输入保持为：

   `(x1, x2, x3)`

4. KAN 输出为单标量接触力，并直接进入已知 RHS。

5. 该测试中，正式训练里原本未知但当前可被视为已知的项（例如 `ks, cs` 及其他已知物理参数）均按真值给定。

6. rollout 后计算 loss，loss 延续 AFM04 当前现有 loss 的结构与主要项。

7. adaptive grid 在本功能性测试中开启。

8. KAN 初始化采用随机。

9. `noise_scale = 0.2`。

10. 当前测试只服务于 KAN 功能性验证，不直接替代 AFM04 主线。
